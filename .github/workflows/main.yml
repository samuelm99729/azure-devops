name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    # 1️⃣ Checkout the repository
    - name: Checkout code
      uses: actions/checkout@v3

    # 2️⃣ Set up Python environment
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    # 3️⃣ Install dependencies
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    # 4️⃣ Run unit tests
    - name: Run tests
      run: |
        python -m pytest tests/ -v

    # 5️⃣ Build Docker image
    - name: Build Docker image
      run: |
        docker build -t feedback-app:${{ github.sha }} .

    # 6️⃣ Login to Azure Container Registry (ACR)
    - name: Log in to Azure Container Registry
      uses: azure/docker-login@v1
      with:
        login-server: ${{ secrets.AZURE_REGISTRY_NAME }}.azurecr.io
        username: ${{ secrets.AZURE_CLIENT_ID }}
        password: ${{ secrets.AZURE_CLIENT_SECRET }}

    # 7️⃣ Push Docker image to ACR
    - name: Push Docker image to ACR
      run: |
        docker tag feedback-app:${{ github.sha }} ${{ secrets.AZURE_REGISTRY_NAME }}.azurecr.io/feedback-app:${{ github.sha }}
        docker tag feedback-app:${{ github.sha }} ${{ secrets.AZURE_REGISTRY_NAME }}.azurecr.io/feedback-app:latest
        docker push ${{ secrets.AZURE_REGISTRY_NAME }}.azurecr.io/feedback-app:${{ github.sha }}
        docker push ${{ secrets.AZURE_REGISTRY_NAME }}.azurecr.io/feedback-app:latest

    # 8️⃣ Login to Azure
    - name: Login to Azure
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    # 9️⃣ Set AKS context (connect GitHub runner to your AKS cluster)
    - name: Set AKS Context
      uses: azure/aks-set-context@v3
      with:
        resource-group: feedback-rg
        cluster-name: feedback-aks
        subscription: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

    # 🔟 Deploy updated image to AKS
    - name: Deploy to AKS
      run: |
        echo "🚀 Updating AKS deployment with new image tag: ${{ github.sha }}"

        # Update deployment image to the new build
        kubectl set image deployment/feedback-app-deployment \
          feedback-app=${{ secrets.AZURE_REGISTRY_NAME }}.azurecr.io/feedback-app:${{ github.sha }}

        # Re-apply the service (in case it was changed)
        kubectl apply -f k8s/service.yaml

        # Wait for rollout to finish
        kubectl rollout status deployment/feedback-app-deployment

        echo "✅ Deployment updated successfully!"
