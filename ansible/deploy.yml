---
- name: Deploy Feedback App to AKS
  hosts: localhost
  connection: local
  tasks:
    - name: Apply Kubernetes deployment
      kubernetes.core.k8s:
        state: present
        src: ../k8s/deployment.yaml

    - name: Apply Kubernetes service
      kubernetes.core.k8s:
        state: present
        src: ../k8s/service.yaml
